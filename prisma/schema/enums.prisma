enum ActiveStatus {
    ACTIVE
    BLOCKED
}

enum Role {
    USER
    AUTHOR
    ADMIN
}

enum PostStatus {
    DRAFT
    PUBLISHED
    ARCHIVE
}

enum CommentStatus {
    APPRIVED
    REJECT
}

enum SubcriptionStatus {
    ACTIVE
    CANCELED
    EXPIRED
}
